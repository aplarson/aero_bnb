<form class="form-inline" id="filter-form">

  <div id="airport-select"></div>

  <div id="date-filter">
    <div class="form-group">
      <label>Departure Date
        <input type="date" 
               class="form-control" 
               name="departure_date" 
               value="<%= params.departure_date %>">
      </label>
    </div>

    <div class="form-group">
      <label>Passengers
        <input type="number" 
               class="form-control" 
               name="passengers" 
               value="<%= params.passengers %>">
      </label>
    </div>
  </div>
  <div>
    <div id="price-filter">
    </div>
    <p>
      <span id="price-range">From $<%= params.min_price || 0 %> to $<%= params.max_price || 2000 %></span>
    </p>
  </div>
  <div class="form-group">
    <input type="hidden" 
           id="min-price" 
           name="min_price"
           value="<%= params.min_price %>">
    <input type="hidden" 
           id="max-price" 
           name="max_price"
           value="<%= params.max_price %>">

  </div>

</form>